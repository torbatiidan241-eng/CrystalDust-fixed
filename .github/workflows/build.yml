name: Build GBA ROM

on:
  push:
    branches:
      - main
      - progrees
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential gcc-arm-none-eabi binutils-arm-none-eabi libpng-dev pkg-config libz-dev

      - name: Prepare Toolchain
        run: |
<<<<<<< HEAD
          # 1. Ensure agbcc is executable
          chmod +x tools/agbcc/agbcc
          
          # 2. Setup the directory structure the Makefile expects
          mkdir -p tools/agbcc/bin
          ln -sf "$(pwd)/tools/agbcc/agbcc" "$(pwd)/tools/agbcc/bin/agbcc"

      - name: Build ROM
        run: |
          # Add the tools directory to PATH
          export PATH="$(pwd)/tools:$PATH"
          
          # Clean and Build
          make clean
          make -j$(nproc) CC=arm-none-eabi-gcc \
                        CXX=arm-none-eabi-g++ \
                        AS=arm-none-eabi-as \
                        LD=arm-none-eabi-ld \
                        OBJCOPY=arm-none-eabi-objcopy \
                        CPPFLAGS="-Iinclude"
        
=======
          # Use standard system compilers for tools to ensure they run on Linux
          for dir in tools/*/; do
            if [ -f "${dir}Makefile" ]; then
              make -C "$dir" CC=gcc CXX=g++
            fi
          done

      - name: Build ROM
        run: |
          # Ensure current directory tools are in PATH
          export PATH=$PATH:$(pwd)/tools
          
          # Clean and build using ARM cross-compiler variables
          make clean
          make -j$(nproc) CC=arm-none-eabi-gcc \
                        CXX=arm-none-eabi-g++ \
                        LD=arm-none-eabi-ld \
                        AS=arm-none-eabi-as \
                        OBJCOPY=arm-none-eabi-objcopy \
                        AR=arm-none-eabi-ar

>>>>>>> 2ca0950f8d7375c27348b73c5f7694a59ecc6b84
      - name: Upload ROM artifact
        uses: actions/upload-artifact@v4
        with:
          name: rom-artifact
          path: "*.gba"